// Este archivo se genera con `bun run supabase:types` (requiere SUPABASE_PROJECT_ID
// en .env y `supabase login` hecho). Placeholder mínimo para que el resto del
// código compile hasta la primera generación real contra el proyecto compartido
// con tuDeclaracion — NO es un schema inventado, es un stub honesto.
//
// Ver .claude/skills/project-bootstrap/references/supabase-types-setup.md (tuDeclaracion)
// para el procedimiento completo.

export interface Database {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
  }
}
