// Skeleton de página. Listado/búsqueda de contadores vía Supabase Admin API
// (src/modules/usuarios/, ADR-0003) + block/unblock nativo — lógica real en
// /sprint-development, no acá (bootstrap solo deja el esqueleto de ruta).
export default function UsuariosPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Usuarios</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Listado de contadores, vía Supabase Admin API — pendiente de implementación
        (ver src/modules/usuarios/).
      </p>
    </div>
  );
}
