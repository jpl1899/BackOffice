// Skeleton de página. CRUD genérico de codigo_linea_1376 / codigo_iva
// (src/modules/catalogos/), vía service-role client (ADR-0002).
export default function CatalogosPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Catálogos</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        CRUD de codigo_linea_1376 / codigo_iva — pendiente de implementación
        (ver src/modules/catalogos/).
      </p>
    </div>
  );
}
