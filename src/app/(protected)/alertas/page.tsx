// Skeleton de página. Alertas computadas on-read: declaraciones con_errores +
// errores repetidos de Sentry (src/modules/alertas/) — sin tabla propia.
export default function AlertasPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Alertas</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Declaraciones en estado con_errores + errores repetidos (Sentry) — pendiente de
        implementación (ver src/modules/alertas/).
      </p>
    </div>
  );
}
